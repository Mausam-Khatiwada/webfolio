// This component is no longer needed since we're removing the hamburger menu
// We'll keep an empty component for now to avoid breaking imports
export function MobileSidebarDrawer() {
  return null;
}
